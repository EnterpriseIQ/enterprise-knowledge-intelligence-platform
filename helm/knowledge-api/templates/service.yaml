apiVersion: v1
kind: Service
metadata:
  name: {{ include "knowledge-api.fullname" . }}
  labels:
    {{- include "knowledge-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "knowledge-api.selectorLabels" . | nindent 4 }}
